{
  "private": true,
  "devDependencies": {
    "browserify": ">=10.2.0",
    "watchify": ">=3.2.1",
    "reactify": ">=1.1.1",
    "react": ">=0.13.3"
  },
  "browserify": {
    "transform": [
      ["reactify"]
    ]
  },
  "scripts": {
    "prepublish": "browserify main.js -o bundle.js",
    "start": "python -m SimpleHTTPServer",
    "watch": "./node_modules/.bin/watchify main.js -o bundle.js --debug --verbose"
  }
}